# ── PyTorch (CUDA 12.1) ──────────────────────────────────────────────────
# Installed via setup_env.sh with --index-url; listed here for reference.
# pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121 \
#     --index-url https://download.pytorch.org/whl/cu121

# ── Core model stack ─────────────────────────────────────────────────────
transformers==4.45.2
tokenizers==0.20.3
sentencepiece==0.1.99
safetensors==0.7.0
peft==0.10.0
accelerate==0.34.2
bitsandbytes==0.41.0
deepspeed==0.13.5
timm==0.9.12
einops==0.6.1
einops-exts==0.0.4
internvl_chat==2.0.0

# ── Data / image handling ─────────────────────────────────────────────────
datasets==4.8.5
numpy==2.4.6
pandas==3.0.2
pillow==12.1.1
opencv-python==4.11.0.86
scikit-image==0.26.0
scipy==1.17.1
scikit-learn==1.8.0
decord==0.6.0

# ── Evaluation ────────────────────────────────────────────────────────────
pycocoevalcap==1.2
pycocotools==2.0.11
language_evaluation==0.1.0
nltk==3.9.4
openai==2.34.0

# ── Training infra ────────────────────────────────────────────────────────
wandb==0.25.1
tensorboardX==2.6.5

# ── Utilities ─────────────────────────────────────────────────────────────
huggingface_hub==0.36.2
matplotlib==3.10.8
tqdm==4.67.3
pyyaml==6.0.3
packaging==26.2
regex==2026.5.9
requests==2.34.2
psutil==7.2.2
ninja==1.13.0

# ── flash-attn ────────────────────────────────────────────────────────────
# Installed separately in setup_env.sh (requires --no-build-isolation).
# flash_attn==2.6.3+cu128torch2.5
